# Interface change requests

No outstanding request is recorded at handoff creation.

Each future request: ID, requester, current schema/version, proposed change, reason, callers affected, fixture, migration, coordinator decision and commit. Do not silently patch another agent's contract.
