# Paste this into the coordinating coding agent

Implement the research project in this fresh repository from the supplied handoff. Read README.md, AGENTS.md, MEMRL_SPEC.md revision3, docs/CONTINUATION_METHOD.md, docs/NOVELTY_AND_VENUE.md, docs/INTERFACES.md, docs/PARALLEL_BUILD.md and docs/ACCEPTANCE_GATES.md before changing interfaces.

You are authorized to organize parallel implementation agents according to the ownership table. Complete routine implementation, integration and fixes autonomously. Preserve this specification, reference core, fixtures and research records. Do not delete data or checkpoints. Work in isolated branches/worktrees and integrate small verified changes. If parallel agents are unavailable, execute the same packets sequentially.

First run the supplied CPU tests and handoff validator. Implement and freeze production contracts and config validation. Assign packets 01–06 in Wave1; retain packet00 yourself. Use schema fixtures to unblock CPU work before GPU engines exist. Reconcile interface requests centrally, and never allow duplicate competing implementations of the store, tokenizer budgeter, scorer or loss normalizer.

The desired final product is a tested production package under src/memrl, not a restatement of the plan. Implement every core production CLI required by the specification and GPU runbook. Missing components must cause clear failures. Mock backends may exist only in labeled tests. Never generate fake experiment results, fabricate timing measurements, silently use final-test data for selection, or call a hypothetical API without verifying it.

The machine has four H100s; inventory the actual variant, memory and interconnect. One scheduler owns all device leases. Probe and pin model/library/container revisions, establish real logprob/adapter parity, verify one-update and two-rank loss equivalence, test resume, then execute the declared development pilot. Resolve OOM with scheduling and batching first; scientific budget changes require a new protocol record.

Do not run the full experiment grid until the pilot measures capacity pressure, strong baselines and resource costs. Default total planning envelope is 1,500 allocated GPU-hours, not a prediction; charge every allocated device during idle phases too. Do not exceed the configured envelope. The final test lock opens only after the freeze gate is recorded. A claim of novel learning requires the matched F03 versus F02 control to survive; no publication is guaranteed.

Keep records/IMPLEMENTATION_STATUS.md current. Report exact executed checks, evidence artifacts, remaining blockers and the next dependency gate. Finish the authorized core implementation and development validation; do not stop after creating file stubs. If actual hardware or credentials are unavailable, finish all independent CPU work and report precisely which GPU evidence remains unavailable.
