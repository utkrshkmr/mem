[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "memrl-contract-reference"
version = "0.1.0"
description = "CPU reference contracts for continuation-aware bounded-memory research"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

